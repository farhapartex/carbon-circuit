import { PageHeader } from "@/components/shared/PageHeader";

export default function NotificationsPage() {
  return (
    <PageHeader
      title="Notifications"
      description="Everything that has happened across your organization."
    />
  );
}
