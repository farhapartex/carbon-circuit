import { PageHeader } from "@/components/shared/PageHeader";

export default function SolutionsTraceabilityPage() {
  return (
    <PageHeader
      title="Traceability"
      description="Prove where every component came from."
    />
  );
}
