import { PageHeader } from "@/components/shared/PageHeader";

export default function OrganizationSettingsPage() {
  return (
    <PageHeader
      title="Organization settings"
      description="Profile, product categories, and verification status."
    />
  );
}
