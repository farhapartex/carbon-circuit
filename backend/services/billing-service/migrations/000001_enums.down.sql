DROP TYPE IF EXISTS billing.idempotency_state;
DROP TYPE IF EXISTS billing.webhook_state;
DROP TYPE IF EXISTS billing.invoice_status;
DROP TYPE IF EXISTS billing.usage_dimension;
DROP TYPE IF EXISTS billing.subscription_state;
DROP TYPE IF EXISTS billing.organization_type;
DROP TYPE IF EXISTS billing.plan_tier;
