import { PageHeader } from "@/components/shared/PageHeader";

export default function BatchesPage() {
  return (
    <PageHeader
      title="Batches"
      description="Every batch your organization has produced or handled."
    />
  );
}
