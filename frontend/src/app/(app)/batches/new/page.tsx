import { PageHeader } from "@/components/shared/PageHeader";

export default function BatchesNewPage() {
  return (
    <PageHeader
      title="Create a batch"
      description="Register a produced quantity of a component."
    />
  );
}
